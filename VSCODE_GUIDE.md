# Работа с проектом F411CE в VS Code

Подробное руководство по редактированию, сборке и отладке STM32-проекта в Visual Studio Code.

---

## 📋 Содержание

1. [Требования](#требования)
2. [Настройка окружения](#настройка-окружения)
3. [Структура проекта](#структура-проекта)
4. [Сборка проекта](#сборка-проекта)
5. [Прошивка контроллера](#прошивка-контроллера)
6. [Отладка](#отладка)
7. [Полезные команды](#полезные-команды)
8. [Горячие клавиши](#горячие-клавиши)
9. [Troubleshooting](#troubleshooting)

---

## Требования

### Обязательное ПО

1. **Visual Studio Code** — [скачать](https://code.visualstudio.com/)
2. **ARM GCC Toolchain** — встроен в STM32CubeIDE или [GNU Arm Embedded Toolchain](https://developer.arm.com/downloads/-/gnu-rm)
3. **Make** — для Windows через MinGW или Git Bash
4. **OpenOCD** — для прошивки и отладки
5. **ST-Link drivers** — [STSW-LINK009](https://www.st.com/en/development-tools/stsw-link009.html)

### Рекомендуемые расширения VS Code

```bash
# C/C++ IntelliSense
code --install-extension ms-vscode.cpptools

# Cortex-Debug (для отладки STM32)
code --install-extension marus25.cortex-debug

# STM32 VS Code Extension (опционально)
code --install-extension bmd.stm32-for-vscode
```

---

## Настройка окружения

### 🎯 Быстрая настройка (рекомендуется)

✅ **Для этого проекта PATH уже настроен!**

Файл `.vscode/tasks.json` автоматически добавляет пути к toolchain из STM32CubeIDE 1.17.0, поэтому сборка будет работать сразу через **Ctrl+Shift+B** без ручной настройки системного PATH.

> **Текущая конфигурация**: STM32CubeIDE 1.17.0 (обнаружен в `C:\ST\`)

---

### Windows: добавление toolchain в PATH (опционально)

Если хотите использовать toolchain глобально (в любом терминале), добавьте пути вручную:

1. Откройте **Параметры системы** → **Переменные среды**
2. Найдите переменную `Path` и добавьте:

```plaintext
C:\ST\STM32CubeIDE_1.17.0\STM32CubeIDE\plugins\com.st.stm32cube.ide.mcu.externaltools.gnu-tools-for-stm32.12.3.rel1.win32_1.1.0.202410251130\tools\bin

C:\ST\STM32CubeIDE_1.17.0\STM32CubeIDE\plugins\com.st.stm32cube.ide.mcu.externaltools.make.win32_2.2.0.202409170845\tools\bin

C:\ST\STM32CubeIDE_1.17.0\STM32CubeIDE\plugins\com.st.stm32cube.ide.mcu.externaltools.openocd.win32_2.4.100.202409170845\tools\bin
```

⚠️ **Важно**: пути зависят от версии STM32CubeIDE. Найдите актуальные пути в вашей установке через поиск в `C:\ST\`.

### Проверка установки

Откройте терминал (CMD или PowerShell) и выполните:

```bash
arm-none-eabi-gcc --version
make --version
openocd --version
```

Все команды должны вернуть версии программ.

> **Примечание**: Если команды не найдены, но `.vscode/tasks.json` настроен правильно, сборка через VS Code (Ctrl+Shift+B) всё равно будет работать.

---

## Структура проекта

```
F411CE/
├── .vscode/                    # Конфигурация VS Code
│   ├── c_cpp_properties.json   # IntelliSense (пути к заголовкам)
│   ├── tasks.json              # Задачи сборки/прошивки
│   ├── launch.json             # Конфигурация отладчика
│   └── settings.json           # Настройки проекта
├── Core/
│   ├── Inc/                    # Заголовочные файлы
│   └── Src/                    # Исходники (main.c, uart.c, fram.c...)
├── Drivers/                    # HAL драйверы STM32
├── Middlewares/                # FreeRTOS
├── Debug/                      # Выходные файлы сборки
│   ├── makefile                # Автогенерируемый Makefile
│   └── primGPT_F411.elf        # Прошивка после сборки
├── F411CE.ioc                  # STM32CubeMX проект
├── STM32F411CEUX_FLASH.ld      # Linker script (Flash)
├── Run.cfg                     # OpenOCD конфигурация
├── CLAUDE.md                   # Инструкции для AI-ассистента
└── VSCODE_GUIDE.md             # Это руководство
```

---

## Сборка проекта

### ⚠️ Первая сборка (обязательно!)

**ВАЖНО**: Перед первой сборкой в VS Code необходимо один раз собрать проект в STM32CubeIDE.

#### Почему это нужно?

STM32CubeIDE автоматически генерирует папку `Debug/` с полным Makefile, который включает:
- Все HAL драйверы STM32
- FreeRTOS kernel
- Linker script конфигурацию
- Правила компиляции и линковки

#### Шаги для первой сборки:

1. **Откройте проект в STM32CubeIDE**
   - File → Open Projects from File System
   - Выберите папку проекта `F411CE`

2. **Выполните сборку**
   - Project → Build All (Ctrl+B)
   - Дождитесь завершения (первая сборка ~30-60 сек)

3. **Проверьте результат**
   - В папке `Debug/` должны появиться:
     - `makefile` (автогенерируемый)
     - `primGPT_F411.elf` (прошивка)
     - `primGPT_F411.hex`, `.bin`, `.map`, `.list`

4. **Теперь можно работать в VS Code**
   - Закройте STM32CubeIDE (или оставьте открытым для CubeMX)
   - Все последующие сборки выполняются через VS Code

> **Примечание**: Если вы измените конфигурацию в CubeMX (`.ioc` файл) и пересгенерируете код, Makefile в `Debug/` будет пересоздан автоматически.

---

### Через терминал VS Code

После первой сборки (см. выше) используйте команды:

```bash
# Полная сборка
make -C Debug all

# Очистка
make -C Debug clean

# Пересборка
make -C Debug clean && make -C Debug all
```

### Через VS Code Tasks (Ctrl+Shift+B)

После создания `tasks.json` (см. ниже) используйте:

- **Ctrl+Shift+B** → выбрать задачу:
  - `Build` — сборка проекта
  - `Clean` — очистка
  - `Clean & Build` — пересборка
  - `Flash` — прошивка через OpenOCD

---

## Прошивка контроллера

### Метод 1: OpenOCD (рекомендуется)

```bash
openocd -f Run.cfg -c "program Debug/primGPT_F411.elf verify reset exit"
```

### Метод 2: ST-Link Utility

Используйте ST-Link Utility GUI для прошивки `Debug/primGPT_F411.hex`.

### Метод 3: VS Code Task

Настройте task `Flash` в `tasks.json` и запускайте через палитру команд (Ctrl+Shift+P → `Tasks: Run Task` → `Flash`).

---

## Отладка

### Cortex-Debug (рекомендуется)

1. Установите расширение **Cortex-Debug**:
   ```bash
   code --install-extension marus25.cortex-debug
   ```

2. Создайте `launch.json` (уже настроен в проекте ✅)

3. Подключите ST-Link к плате

4. Нажмите **F5** или откройте вкладку **Run and Debug** (Ctrl+Shift+D) и выберите конфигурацию **Debug (OpenOCD)**

📘 **Подробное руководство по отладке:** см. [DEBUG_GUIDE.md](DEBUG_GUIDE.md) — полное руководство для начинающих с примерами.

### Возможности отладчика

- **Breakpoints** — точки останова (клик на номере строки)
- **Step Over (F10)** — выполнить строку
- **Step Into (F11)** — войти в функцию
- **Step Out (Shift+F11)** — выйти из функции
- **Continue (F5)** — продолжить выполнение
- **Variables** — просмотр переменных
- **Watch** — отслеживание выражений
- **Call Stack** — стек вызовов
- **Peripheral Registers** — просмотр регистров периферии (SVD файл)

---

## Конфигурационные файлы

### `.vscode/tasks.json`

Создайте файл для автоматизации сборки:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "command": "make",
      "args": ["-C", "Debug", "all", "-j8"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      }
    },
    {
      "label": "Clean",
      "type": "shell",
      "command": "make",
      "args": ["-C", "Debug", "clean"],
      "problemMatcher": []
    },
    {
      "label": "Clean & Build",
      "dependsOrder": "sequence",
      "dependsOn": ["Clean", "Build"],
      "problemMatcher": []
    },
    {
      "label": "Flash",
      "type": "shell",
      "command": "openocd",
      "args": [
        "-f",
        "Run.cfg",
        "-c",
        "program Debug/primGPT_F411.elf verify reset exit"
      ],
      "problemMatcher": [],
      "presentation": {
        "reveal": "always"
      }
    },
    {
      "label": "Build & Flash",
      "dependsOrder": "sequence",
      "dependsOn": ["Build", "Flash"],
      "problemMatcher": []
    }
  ]
}
```

### `.vscode/launch.json`

Конфигурация для отладки через Cortex-Debug:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (OpenOCD)",
      "cwd": "${workspaceFolder}",
      "executable": "${workspaceFolder}/Debug/primGPT_F411.elf",
      "request": "launch",
      "type": "cortex-debug",
      "runToEntryPoint": "main",
      "servertype": "openocd",
      "configFiles": ["Run.cfg"],
      "searchDir": ["${workspaceFolder}"],
      "svdFile": "${workspaceFolder}/Drivers/CMSIS/Device/ST/STM32F4xx/Include/STM32F411xE.svd",
      "preLaunchTask": "Build",
      "showDevDebugOutput": "raw"
    },
    {
      "name": "Attach (OpenOCD)",
      "cwd": "${workspaceFolder}",
      "executable": "${workspaceFolder}/Debug/primGPT_F411.elf",
      "request": "attach",
      "type": "cortex-debug",
      "servertype": "openocd",
      "configFiles": ["Run.cfg"],
      "searchDir": ["${workspaceFolder}"],
      "svdFile": "${workspaceFolder}/Drivers/CMSIS/Device/ST/STM32F4xx/Include/STM32F411xE.svd"
    }
  ]
}
```

⚠️ **Примечание**: если SVD файл отсутствует, удалите строку `"svdFile": ...` или скачайте [STM32F411xE.svd](https://github.com/posborne/cmsis-svd/blob/master/data/STMicro/STM32F411.svd).

---

## Полезные команды

### Терминальные команды

```bash
# Сборка с подробным выводом
make -C Debug all VERBOSE=1

# Параллельная сборка (быстрее)
make -C Debug all -j8

# Получить размер прошивки
arm-none-eabi-size Debug/primGPT_F411.elf

# Дизассемблирование
arm-none-eabi-objdump -d Debug/primGPT_F411.elf > disassembly.txt

# Просмотр карты памяти
cat Debug/primGPT_F411.map | less

# Генерация HEX из ELF
arm-none-eabi-objcopy -O ihex Debug/primGPT_F411.elf Debug/primGPT_F411.hex

# Генерация BIN из ELF
arm-none-eabi-objcopy -O binary Debug/primGPT_F411.elf Debug/primGPT_F411.bin
```

### OpenOCD команды

```bash
# Запуск GDB сервера (отладка без VS Code)
openocd -f Run.cfg

# Сброс платы
openocd -f Run.cfg -c "init; reset; exit"

# Чтение Flash памяти
openocd -f Run.cfg -c "init; flash read_bank 0 flash_dump.bin 0 0x80000; exit"
```

---

## Горячие клавиши

### Редактирование

| Клавиша | Действие |
|---------|----------|
| `Ctrl+Space` | Автодополнение |
| `F12` | Перейти к определению |
| `Shift+F12` | Найти все ссылки |
| `Ctrl+.` | Быстрое исправление |
| `F2` | Переименовать символ |
| `Ctrl+K Ctrl+0` | Свернуть все |
| `Ctrl+K Ctrl+J` | Развернуть все |

### Сборка и отладка

| Клавиша | Действие |
|---------|----------|
| `Ctrl+Shift+B` | Запустить задачу сборки |
| `F5` | Начать отладку |
| `Ctrl+F5` | Запустить без отладки |
| `Shift+F5` | Остановить отладку |
| `F9` | Установить/снять breakpoint |
| `F10` | Step Over (следующая строка) |
| `F11` | Step Into (войти в функцию) |
| `Shift+F11` | Step Out (выйти из функции) |

### Навигация

| Клавиша | Действие |
|---------|----------|
| `Ctrl+P` | Быстрый переход к файлу |
| `Ctrl+Shift+F` | Поиск в проекте |
| `Ctrl+G` | Перейти к строке |
| `Alt+Left/Right` | Назад/Вперёд по истории |
| `Ctrl+Tab` | Переключение между файлами |

---

## Troubleshooting

### Проблема: `arm-none-eabi-gcc not found`

**Решение:**
1. Убедитесь, что toolchain установлен
2. Добавьте путь к `arm-none-eabi-gcc` в PATH (см. раздел "Настройка окружения")
3. Перезапустите VS Code после изменения PATH

### Проблема: `make: command not found`

**Решение:**
- **Windows**: установите Make через STM32CubeIDE или [MinGW](https://www.mingw-w64.org/)
- **Linux/macOS**: `sudo apt install build-essential` или `brew install make`

### Проблема: `openocd` не находит ST-Link

**Решение:**
1. Установите [ST-Link drivers](https://www.st.com/en/development-tools/stsw-link009.html)
2. Проверьте подключение: `openocd -f Run.cfg`
3. Убедитесь, что ST-Link не занят другой программой (STM32CubeIDE)

### Проблема: IntelliSense не работает

**Решение:**
1. Откройте Command Palette (Ctrl+Shift+P)
2. Выполните: `C/C++: Reset IntelliSense Database`
3. Перезапустите VS Code
4. Проверьте `.vscode/c_cpp_properties.json` — должны быть указаны правильные пути к заголовочным файлам

### Проблема: отладчик не останавливается на breakpoint

**Решение:**
1. Убедитесь, что проект собран с флагами `-g3 -O0` (Debug конфигурация)
2. Проверьте, что прошивка актуальная (пересоберите и прошейте заново)
3. В `launch.json` добавьте: `"runToEntryPoint": "main"`

### Проблема: ошибка "No space left on device" при сборке

**Решение:**
- Проверьте свободное место на диске
- Выполните `make -C Debug clean` для очистки временных файлов

### Проблема: сборка проходит, но прошивка не запускается

**Решение:**
1. Проверьте linker script (`STM32F411CEUX_FLASH.ld`)
2. Убедитесь, что OpenOCD корректно прошил: должна быть строка `verified XXX bytes`
3. Сделайте полный сброс: `openocd -f Run.cfg -c "init; reset halt; reset; exit"`

---

## Дополнительные ресурсы

- [STM32CubeIDE Documentation](https://www.st.com/en/development-tools/stm32cubeide.html)
- [ARM GCC Toolchain](https://developer.arm.com/tools-and-software/open-source-software/developer-tools/gnu-toolchain/gnu-rm)
- [OpenOCD Documentation](https://openocd.org/doc/)
- [Cortex-Debug Extension](https://github.com/Marus/cortex-debug)
- [FreeRTOS Documentation](https://www.freertos.org/Documentation/RTOS_book.html)

---

## Сравнение STM32CubeIDE vs VS Code

| Критерий | STM32CubeIDE | VS Code |
|----------|--------------|---------|
| **Простота настройки** | ✅ Всё из коробки | ⚠️ Требует настройки |
| **Скорость работы** | ⚠️ Медленный (Eclipse) | ✅ Быстрый |
| **Кастомизация** | ❌ Ограничена | ✅ Полная свобода |
| **IntelliSense** | ⚠️ Средний | ✅ Отличный |
| **Отладка** | ✅ Встроенная | ✅ Через Cortex-Debug |
| **CubeMX интеграция** | ✅ Нативная | ⚠️ Через .ioc файл |
| **Git интеграция** | ⚠️ Базовая | ✅ Отличная |
| **Расширения** | ❌ Ограничены | ✅ Огромная экосистема |

**Рекомендация:** используйте STM32CubeIDE для генерации конфигурации (CubeMX), а VS Code для разработки и отладки.

---

## Workflow: CubeMX + VS Code

1. **Настройка периферии**: откройте `F411CE.ioc` в STM32CubeMX → измените конфигурацию → Generate Code
2. **Редактирование**: работайте с кодом в VS Code (добавляйте логику в `USER CODE` блоки)
3. **Сборка**: `Ctrl+Shift+B` → Build
4. **Прошивка**: Task `Flash` или через терминал
5. **Отладка**: `F5` → Debug с breakpoints

---

*Последнее обновление: 2026-02-16*
